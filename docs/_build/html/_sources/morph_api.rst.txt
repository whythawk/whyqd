Morph
=====

The `Morph` class defines the restructuring actions performed on a single table in a :doc:`method`. 
`BaseMorph` is the main definition for an action and is inherited by child classes in the `/morph` 
folder.

.. autoclass:: core.BaseMorph
	:member-order: bysource
	:members: