Action
======

The `Action` class defines the restructuring actions performed in a :doc:`method`. `BaseAction` is the main definition
for an action and is inherited by child classes in the `/action` folder.

.. autoclass:: core.BaseAction
	:member-order: bysource
	:members: